{
  "buildCommand": "cd \"Client leger\" && npm install && npm run build",
  "outputDirectory": "Client leger/.next",
  "installCommand": "cd \"Client leger\" && npm install",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/Client leger/$1"
    }
  ]
}
